let operando1 = 23;
let operando1 = 33;
console.log(operando1);

// Esta declaración no es correcto, dado que al asignarle el valor 23 a operando1 ya estamos declarando la variable con let.
// Pero al otorgarle el valor de 33, estamos declarando nuevamente, lo que sería una redundancia.